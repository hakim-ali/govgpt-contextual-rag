version: '3.8'

services:
  govgpt-api:
    build: .
    container_name: govgpt-api-openwebui
    ports:
      - "8100:8100"
    volumes:
      # Mount artifacts and documents from host
      - ./artifacts:/app/artifacts:ro
    env_file:
      - .env
    environment:
      # Override specific Docker environment settings
      - ARTIFACT_DIR=/app/artifacts
      - PYTHONUNBUFFERED=1
      - PYTHONPATH=/app
    restart: unless-stopped
    healthcheck:
      test: ["CMD", "curl", "-f", "http://localhost:8100/health"]
      interval: 30s
      timeout: 100s
      retries: 3
      start_period: 40s
    networks:
      - govgpt-network

networks:
  govgpt-network:
    driver: bridge

# Optional: Add volumes for persistence if needed
# volumes:
#   artifacts:
#   documents: